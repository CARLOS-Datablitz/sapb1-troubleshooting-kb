=================================================
1. Preparar el entorno en el sles, HANA, HANA-MT:
# Empecemos.....
screen -dRR
cd /tmp && ls -lha
mkdir db_fakt_1 FAKT_ZIP/
chmod -R 777 db_fakt_1/ FAKT_ZIP
chown -R ndbadm:sapsys db_fakt_1/ FAKT_ZIP/ && ls -lha

=================================================
2. Variables de entorno:
-------------------   
2.1 Sacar información de las variables:
sld:~ # 
cat Desktop/SAP_Configuration.txt
2.2 Ingresar las variables en el sles:/tmp #
# Varibles HANASQL (B1SYSTEM)
# en donde confirmo la ruta o parametro de la siguiente línea.?
dbSql="/hana/shared/NDB/hdbclient/hdbsql"
dbInstance="00"
dbHost="127.0.0.1"
dbTenant="BO6"
dbUser="B1SYSTEM"
dbPass="RQ5dp8qS"

-------------------   
# ver las db o schemas: entrar al rds/Business One 64 bits

# Testear la base de datos: NDB
sles:/tmp #
"$dbSql" -i "$dbInstance" -d "$dbTenant" -n "$dbHost" -u "$dbUser" -p "$dbPass" 'select * from "SYS"."SCHEMAS";'

# Export the schema
sles:/tmp #
"$dbSql" -i "$dbInstance" -d "$dbTenant" -n "$dbHost" -u "$dbUser" -p "$dbPass" 'export 'TEST_FAKTSOLUTION'."*" as binary into '\'/tmp/db_fakt_1\'' with ignore existing threads 10;'


# import and rename the schema
"$dbSql" -i "$dbInstance" -d "$dbTenant" -n "$dbHost" -u "$dbUser" -p "$dbPass" 'import 'SBO_APC'."*" as binary from '\'/tmp/db_fakt_1\'' with ignore existing threads 10 rename schema "SBO_APC" to "TEST_SI";'

rm -R db_fakt_1/ FAKT_ZIP/

  =========================  FIN =================
tar -czvf /tmp/FAKT_ZIP/TEST_FAKTSOLUTION.tar.gz -C /tmp/db_fakt_1 .

rm -rf /tmp/db_fakt_1/* /tmp/db_fakt_1/.*

sftp -P 2022 c.pecho@files.dctpa.privatcloud.biz
sftp -P 2022 c.pecho@files.dcdus.privatcloud.biz
C4!qE0e]cVFy&$m 
cd /ramo/APC
ls -lha
get 080SBOMAQEMAG25072025.tar.xz
bye


